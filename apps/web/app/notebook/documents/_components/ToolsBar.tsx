"use client";

import React from 'react';
import { nanoid } from '@/lib/utils'
import Chat from './Chat';


const ToolsBar = () => {

    return (
        <Chat />
      );
};

export default ToolsBar;
